#!/bin/bash

# This repository contains only documentation, no code to build
echo "This repository contains only documentation (README.md), no actual code to build."

# Install tools for markdown linting and spell checking
npm install -g markdownlint-cli
npm install -g cspell
